@model SomeNewTest.ViewModels.ConfirmEmailViewModel

@{
    ViewData["Title"] = "Confirm Email";
}

<h2>@ViewData["Title"]</h2>

<div>
    @if (Model.IsSuccess)
    {
        <p>Your email has been confirmed.</p>
    }
    else
    {
        <p>Failed to confirm email.</p>
    }
</div>
